// =====================================================================
// DocumentTree — configuration (your ".env")
// =====================================================================
// Edit the values below, then host index.html anywhere. This file MUST be
// loaded BEFORE documentTree.js (see index.html). Nothing here is secret — the
// share token is a public, read-only access key, the same one in the URL.
// =====================================================================
window.DOCUMENTTREE_CONFIG = {
    //   Example: "https://documenttree.nukepages.net"
    apiBase: "",
    //   Example: "hjpLzMZufHKe118xVpQiiw5"
    token: ""
};
